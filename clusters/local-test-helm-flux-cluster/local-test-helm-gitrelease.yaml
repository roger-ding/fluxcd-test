---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: local-test
  namespace: default 
spec:
  interval: 15s
  chart:
    spec:
      chart: ./clusters/local-test-helm-flux-cluster/local-test
      sourceRef:
        kind: GitRepository
        name: fluxcd-test
      interval: 15s

